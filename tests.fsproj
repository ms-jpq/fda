<?xml version="1.0" encoding="utf-8" ?>
<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp3.0</TargetFramework>
  </PropertyGroup>

  <ImportGroup>
    <Import Project="src/domain-agnostic/export.targets" Condition=" '$(ImportedDomainAgnostic)' == '' " />
    <Import Project="src/dotnet-exts/export.targets" Condition=" '$(ImportedDotnetExts)' == '' " />
    <Import Project="src/fparsec-exts/export.targets" Condition=" '$(ImportedFParsecExts)' == '' " />
  </ImportGroup>

  <ItemGroup>
    <Compile Include="tests/entry.fs" />
  </ItemGroup>

  <Import Project=".paket\Paket.Restore.targets" />
</Project>
